# Notebooks

Optional exploratory notebooks. The authoritative, reproducible pipeline lives
in `scripts/` and is driven by `make reproduce`. Notebooks here should import
from `scripts/common.py` rather than re-deriving numbers.
